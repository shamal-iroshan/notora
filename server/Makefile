# Start development server with Air (hot reload)
dev:
	air

# Run server without hot reload
run:
	go run ./cmd/notora-server

# Build production binary
build:
	go build -o notora-server ./cmd/notora-server

# Clean temporary build folder
clean:
	rm -rf tmp
